    import { defineConfig } from "@lovable.dev/vite-tanstack-config";

    export default defineConfig({
    // Pastikan bagian ini ada untuk memberi tahu Nitro bahwa kita deploy ke Vercel
    server: {
        preset: "vercel", 
    },
    tanstackStart: {
        server: { entry: "server" },
    },
    });